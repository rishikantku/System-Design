# Copyrighted source book — do not commit (25 MB binary, and not ours to distribute)
*.pdf

# Extracted text from the book
ddia*.txt

# Video build output — frames, audio, segments and the encoded MP4s.
# ~5 GB per course build, all reproducible from video/*/: plan, render, encode.
video/build/
video/build-coding/

# Python bytecode
__pycache__/
*.pyc

# YouTube uploader credentials and progress — never commit these
companies/linkedin/client_secret.json
video/coding/client_secret.json
video/coding/upload-token.json
video/coding/upload-state.json
